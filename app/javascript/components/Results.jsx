/* Maximum of 10 unique candidates will be shown */

//Out of scope for role but would use vote table to render current results

import React from "react";

const Results = () => {
  return <div>Results</div>;
};

export default Results;
